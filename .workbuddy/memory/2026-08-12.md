# 2026-08-12

## README 编码转换
- 原 `README.md` 为 UTF-16 LE + BOM + CRLF（被 git/GitHub 当二进制，diff 不可见，AI 编辑易乱码）。
- 已用 Python 转成 **UTF-8 无 BOM + LF**，内容（中文速查表、8/11 日志）完整保留，提交 `1d2de98`。
- 验证要点：首字节非 BOM、无 CR 残留、`一` 与 `8/11` 均在。
- 注意：本机 `core.autocrlf=true`，工作区会转回 CRLF，但仓库/GitHub 存 LF（GitHub 标准，无碍）。
- 教训：编辑 UTF-16 文件必须用 `raw.decode('utf-16')` 且按 CRLF 处理；直接 UTF-8 写会乱码（此前已踩坑一次，靠 `git checkout` 恢复）。

## 当前推送阻塞点（截至 8/12）
- SSH 公钥尚未添加到 GitHub 账号（本机已生成 `C:\Users\23183\.ssh\id_ed25519`，ssh-agent 服务已启动）。
- 用户需登录 https://github.com/settings/ssh/new 粘贴公钥 `ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIKTxCLSqrIJRf7X7MQLFVAIQ6w3sNZeacyD+WI1QE5R9 sanmuyoulong`。
- 之后本机执行 `ssh -T git@github.com`（显示 Hi sanmuyoulong!）再 `git push -u origin main`。
- 本地 main 领先远端 `1d00784` 共 5 个提交，均为快进（远端是其祖先），push 不会被拒。

## Sowing Success ML 项目（workspace/notebook.ipynb）
- 任务：用土壤指标 N/P/K/ph 多分类预测 crop，并找出最重要单一特征。
- 数据集 `soil_measures.csv`：2200 行，22 类作物每类 100 条（完全均衡），无缺失值。
- 用户要求简化实现：Step2 = `train_test_split`(全部特征，stratify)；Step3 = 逐特征(4个)各建一个 LogisticRegression，预测测试集、算 accuracy、存 dict。
- 环境：经管 Python 3.13.12 venv 装 pandas/sklearn/matplotlib/seaborn/nbformat（路径 `C:\Users\23183\.workbuddy\binaries\python\envs\default`）。
- 执行 notebook 用 `nbclient`，需 `cd` 到 workspace 目录（否则相对路径读不到 csv）。
- 结果：单特征测试准确率 N=0.139 P=0.191 K=0.280 ph=0.098 → 最重要特征是 **K（钾）**。
- 教训：单特征 LR 需 `StandardScaler` 否则 lbfgs 不收敛告警；用户偏好“按指定步骤、别过度设计”。
- 收尾：用户要求“不用随机森林”（已删 `RandomForestClassifier` 导入，原本也没用到），并补 `## 4. Conclusion` markdown 结论小节。整本 notebook 已 `nbclient` 重跑通过，项目按步骤完整结束。
- 文档化：已将项目介绍写入根 `README.md`——开头加 `## 目录`（链接到一~五章 + Learning Progress），末尾加 `## 五、Sowing Success 项目（机器学习选作物）`（背景/思路/关键代码/结果）。README 现按“一~五”编号章节组织。
