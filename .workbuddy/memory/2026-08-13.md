# 2026-08-13

- 在 README.md 新增「六、逻辑回归（Logistic Regression）笔记」章节，并同步更新文首目录。
- 章节内容：核心概念、Sigmoid 定义与性质、对数几率 logit、决策边界、损失函数（负对数似然/交叉熵的 MLE 五步推导）、梯度下降优化方向、与线性回归对比表、sklearn 速记。
- 章节锚点：`#六逻辑回归logistic-regression笔记`（与既有五章节锚点规则一致）。
- 在 README.md 新增「七、t-SNE（t-分布随机邻域嵌入）笔记」与「八、随机过程与随机游走」两个章节，并同步更新文首目录与 Learning Progress（8/13 条目）。
- 今日学习主线：随机过程（定义/样本轨道/布朗运动/泊松过程）、对称随机游走常返性、k-means 应用场景、t-SNE 完整原理（高斯相似度→条件概率→对称化→t 分布→KL 散度→梯度下降）、高斯打分（σ 含义）、数学板块梳理。
- 环境约定：聊天界面用 KaTeX，不支持 `$$` 与 `\lVert`/`\rVert` 等命令，数学公式在聊天里渲染成 SVG 图片；但 README.md 用 GitHub/VSCode（MathJax）渲染，完整 LaTeX 可正常使用。
