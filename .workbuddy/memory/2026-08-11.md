# 2026-08-11

## 将 My TechLearning 文件夹连接 GitHub 仓库
- 目标仓库（公开）：https://github.com/sanmuyoulong/MyTechLearning （已存在提交 `1d00784`）
- 问题：本地 `.git` 的 `main` 变成"无提交"空分支，但工作区文件与远端几乎一致。
- 解决流程（本地 git 已就绪，push 需本机凭据）：
  1. `git remote add origin https://github.com/sanmuyoulong/MyTechLearning.git`
  2. 配置身份：`user.name=sanmuyoulong`，`user.email=sanmuyoulong@users.noreply.github.com`（GitHub 隐私邮箱，会关联到账号）
  3. 远端 ref 因沙箱限制无法持久化，改用 SHA 直接对齐：`git update-ref refs/heads/main <远端SHA>` 再 `git reset`（mixed）使索引同步；仅根目录 `filename.txt`/`liked_songs.txt` 为本地独有，已提交（新提交 `6451e6d`，比远端超前 1）。
  4. 最终由用户在**自己机器**执行 `git push -u origin main`（Git Credential Manager 弹窗登录，或用 PAT）完成快进推送。
- 注意：沙箱内 `git push` 会因无凭据失败（terminal prompts disabled）。本机正常。

## SSH 密钥生成（解决本机 publickey 拒绝）
- 本机 `C:\Users\23183\.ssh` 原本只有 known_hosts，无任何密钥 → push 报 `Permission denied (publickey)`。
- 已生成 ed25519 密钥对：`id_ed25519`（空密码），并启动 Windows `ssh-agent` 服务（自动启动）。
- 公钥（需用户贴到 GitHub → Settings → SSH and GPG keys → New SSH key）：
  `ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIKTxCLSqrIJRf7X7MQLFVAIQ6w3sNZeacyD+WI1QE5R9 sanmuyoulong`
- 用户完成 GitHub 添加后，`ssh -T git@github.com` 应回显 `Hi sanmuyoulong!`，再 `git push -u origin main` 即可快进。
